# Policy Lab app — install with:  pip install -r application/requirements.txt
streamlit>=1.50
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
scikit-learn>=1.3      # propensity + outcome nuisance estimation (common/)
gurobipy>=11.0         # the LP solver behind R-OW / R-O / Regret-* / IPW / DoublyRobust / Oracle
                       #   needs a Gurobi licence (academic licences are free); without it the
                       #   non-LP parts (DGP design + preview) still work, but a Run will fail.
